

## Swap Apify API Key

The `APIFY_API_TOKEN` secret already exists. I'll use the secrets tool to prompt you to enter the new value, which will replace the current one.

**No code changes needed** -- just a secret value update.

